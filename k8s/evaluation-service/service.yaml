apiVersion: v1
kind: Service
metadata:
  name: evaluation-service
  namespace: toggle-master
  labels:
    app: evaluation-service
spec:
  type: ClusterIP
  selector:
    app: evaluation-service
  ports:
    - name: http
      protocol: TCP
      port: 8004
      targetPort: 8004

apiVersion: v1
kind: Service
metadata:
  name: flag-service
  namespace: toggle-master
  labels:
    app: flag-service
spec:
  type: ClusterIP
  selector:
    app: flag-service
  ports:
    - name: http
      protocol: TCP
      port: 8002
      targetPort: 8002

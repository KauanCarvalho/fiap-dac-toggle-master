apiVersion: v1
kind: Service
metadata:
  name: targeting-service
  namespace: toggle-master
  labels:
    app: targeting-service
spec:
  type: ClusterIP
  selector:
    app: targeting-service
  ports:
    - name: http
      protocol: TCP
      port: 8003
      targetPort: 8003

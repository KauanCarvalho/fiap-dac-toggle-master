name: Lint Targeting Service

on:
  push:
    branches: [ "main" ]
    paths:
      - 'local/services/targeting-service/**'
  pull_request:
    branches: [ "main" ]
    paths:
      - 'local/services/targeting-service/**'
  workflow_dispatch:

jobs:
  lint:
    uses: ./.github/workflows/reusable-lint-python.yml
    with:
      service_path: local/services/targeting-service
      python_version: '3.11'

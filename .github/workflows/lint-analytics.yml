name: Lint Analytics Service

on:
  push:
    branches: [ "main" ]
    paths:
      - 'local/services/analytics-service/**'
  pull_request:
    branches: [ "main" ]
    paths:
      - 'local/services/analytics-service/**'
  workflow_dispatch:

jobs:
  lint:
    uses: ./.github/workflows/reusable-lint-python.yml
    with:
      service_path: local/services/analytics-service
      python_version: '3.11'
